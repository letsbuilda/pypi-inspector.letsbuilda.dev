name: "Docker Build and Push"

on:
  push:
    branches:
      - main
    tags:
      - v*

permissions:
  contents: read
  packages: write
  # This is used to complete the identity challenge
  # with sigstore/fulcio.
  id-token: write

jobs:
  build-push:
    uses: darbiadev/.github/.github/workflows/docker-build-push.yaml@1163b50ed46a8aea41806ee86ec2eb50605521ac

  deploy:
    needs: build-push
    runs-on: ubuntu-latest
    steps:
      - name: "Update deployment"
        run: curl --fail-with-body -X POST '${{ secrets.DEPLOY_WEBHOOK }}'
