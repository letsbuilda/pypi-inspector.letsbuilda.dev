name: GitHub Pages - Python Sphinx

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  docs:
    uses: darbiadev/.github/.github/workflows/github-pages-python-sphinx.yaml@1163b50ed46a8aea41806ee86ec2eb50605521ac
